name: urbanfix_mobile
description: Urbanfix civilian mobile app (Flutter).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.5.6
  http: ^1.2.2
  image_picker: ^1.1.2
  geolocator: ^12.0.0
  webview_flutter: ^4.8.0
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
